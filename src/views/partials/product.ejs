
            <main class="main">


                
                <section class="listadoDeProductos">
                    <h3>
                        Dirección: <%= casa.address %>
                    </h3>
                    <img src="/img/products/<%= casa.image.image_name %>" alt="" class="listProductsImage">
                    <p>
                        Cantidad máxima de personas: <%= casa.n_of_people %>
                    </p>
                    <div class="campo">
                        <label>Servicios</label> <br>
                        <label class="check"><input type="checkbox" name="wifi" <%= locals.casa && casa.wifi == 1 ? 'checked' : null %>>
                        Wi-fi</label>
                        <br>
                        <label class="check"><input type="checkbox" name="pool" <%= locals.casa && casa.pool == 1 ? 'checked' : null %>>
                        Pileta</label>
                        <br>
                        <label class="check"><input type="checkbox" name="parking"
                            <%= locals.casa && casa.parking == 1 ? 'checked' : null %>>Estacionamiento</label>
                        <br>
                        <label class="check"><input type="checkbox" name="barbecue"
                            <%= locals.casa && casa.barbecue ? 'checked' : null %>>
                        Parrilla</label>
                    </div>
                    <h5>Precio por noche: $<%= new Intl.NumberFormat('de-DE', { style: 'decimal' }).format(casa.price) %>
                    </h5>

                    <script src="/js/detalleCrud.js"></script>
                </section>
            </main>
            